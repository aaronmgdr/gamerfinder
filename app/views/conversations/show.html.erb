<div class="row">

  <div class="large-10 columns">
  <%= render 'shared/msg_nav'%>
  <h3 class='subheader'>Subject: <%= @conversation.subject   %></h3>

    
    <% messages_in_conversation.each do |msg| %>
      <div class="panel msg">
        <div class="row">
          <div class="small-4 large-3 columns">
            <figure>  
              <%=link_to image_tag(msg.message.sender.xbox_gamer_info.avatar_img_url), user_path(msg.message.sender.id), class: "th radius"%> 
              <figcaption>  
                <p>
                  <%=link_to msg.message.sender.name, user_path(msg.message.sender.id)%>
                </p>
              </figcaption>  
            </figure>   
          </div>
          <div class="small-8 large-9 columns">
            <p><%= msg.message.body %></p>
          </div>
        </div>
      </div>
    <% end %>
    <div style="float:right"><%= link_to ' Reply', new_message_path(conversation: @conversation.id), class: 'fa fa-reply' %>
    </div>
  </div>
</div>
